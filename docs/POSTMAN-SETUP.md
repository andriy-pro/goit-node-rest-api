# 📱 Postman Collection для GoIT Node.js REST API

## 🚀 Швидкий старт

### ⚠️ **Повний посібник з усунення проблем включено нижче**

📖 **Цей документ містить вичерпний посібник з налагодження для всіх поширених проблем**

### 1. Імпорт колекції

1. Відкрийте Postman
2. Натисніть **Import** у верхньому лівому куті
3. Перетягніть файл `docs/postman-collection.json` або використайте **Upload Files**
4. Колекція **"GoIT Node.js REST API - Contacts"** з'явиться у вашому робочому просторі

### 2. Налаштування змінних середовища

Колекція автоматично встановлює `baseUrl` на `http://localhost:3000`, але ви можете налаштувати власне середовище:

1. Створіть нове Environment: **Environments** → **Create Environment**
2. Назвіть його `GoIT Local`
3. Додайте змінні:

| Змінна | Значення | Опис |
|--------|----------|------|
| `baseUrl` | `http://localhost:3000` | Для локального запуску |
| `baseUrl` | `http://172.x.x.x:3000` | WSL-варіант (для прикладу) |

4. Збережіть середовище

### 3. Активація середовища

Після створення середовища обов'язково активуйте його:

1. У верхньому правому куті Postman знайдіть випадаючий список Environment
2. Виберіть ваше створене середовище `GoIT Local`
3. Переконайтеся, що воно активне (має бути відображено в списку)
4. Тепер всі запити використовуватимуть змінні з цього середовища

💡 **Важливо**: Без активації середовища змінна `{{baseUrl}}` не буде замінена на фактичну адресу сервера.

## 📋 Використання колекції

### 1. Базовий workflow

Рекомендований порядок виконання запитів:

1. **📋 Отримати всі контакти** - Перевіряє роботу API
2. **📄 Отримати контакт за ID** - Використовує ID з попереднього запиту
3. **➕ Створити новий контакт** - Створює тестовий контакт
4. **🔄 Оновити контакт** - Оновлює створений контакт
5. **🗑️ Видалити контакт** - Видаляє тестовий контакт

### 2. Автоматичні тести

Кожен запит містить автоматичні тести:

- ✅ **Статус коди** - Перевіряє правильність HTTP статусів
- ✅ **Структура відповіді** - Валідує JSON структуру
- ✅ **Валідація даних** - Перевіряє формати email та телефону
- ✅ **CORS заголовки** - Перевіряє наявність CORS
- ✅ **Час відповіді** - Перевіряє продуктивність (< 2сек)

### 3. Змінні та автоматизація

Колекція використовує змінні для автоматизації:

- `testContactId` - Автоматично встановлюється з першого контакту
- `createdContactId` - Зберігає ID створеного контакту
- `baseUrl` - Базова URL для всіх запитів

## 🧪 Тестові сценарії

### ✅ Позитивні тести

1. **CRUD операції** - Всі основні операції з контактами
2. **Валідація успішних даних** - Створення з правильними даними
3. **Оновлення часткових даних** - PUT з окремими полями

### ❌ Негативні тести

1. **404 помилки** - Неіснуючі ID та маршрути
2. **400 помилки** - Невалідні дані (email, телефон)
3. **Валідація обов'язкових полів** - Відсутні поля

### 🛡️ Middleware тести

1. **CORS заголовки** - Перевірка Cross-Origin Resource Sharing
2. **JSON парсинг** - Обробка JSON запитів
3. **Error handling** - Централізована обробка помилок

## 📊 Моніторинг та звіти

### Test Runner

Запустіть всю колекцію автоматично:

1. Натисніть на колекцію **"GoIT Node.js REST API - Contacts"**
2. Натисніть **Run collection**
3. Налаштуйте параметри:
   - **Iterations:** 1
   - **Delay:** 100ms між запитами
   - **Data:** Не потрібно
4. Натисніть **Run GoIT Node.js REST API - Contacts**

### Результати тестування

Після запуску ви отримаєте:

- 📊 **Загальна статистика** - Кількість пройдених/провалених тестів
- ⏱️ **Час виконання** - Загальний час виконання всіх запитів
- 📋 **Детальні результати** - Результат кожного тесту
- 🐛 **Помилки** - Детальна інформація про провалені тести

## 🔧 Налагодження та усунення проблем

### 📋 **Аналіз результатів тестування**

Якщо ви отримали результат з провалeними тестами (наприклад: **23 пройшли ✅ / 10 провалилися ❌**), скористайтеся цим посібником.

### 🚨 **Ключові проблеми та виправлення:**

#### 🔧 **Проблема 1: Порожні змінні ID**

**Симптом**: URL виглядають як `http://IP:3000/api/contacts/` (без ID)

**Причина**: Postman запускає тести паралельно, змінні `{{testContactId}}` не встигають зберегтися

**✅ Виправлення**: Оновлена колекція тепер має:

- Фіксований `testContactId = "AeHIrLTr6JkxGE6SN-0Rw"`
- Pre-request скрипт для автоматичного встановлення змінних

#### 🔧 **Проблема 2: Некоректна валідація JavaScript**

**Симптом**: Тести валідації помилок падають

**Причина**: Неправильний синтаксис `||` в `expect()`

**✅ Виправлення**:

```javascript
// Було (неправильно):
pm.expect(error.message).to.include('email' || 'електронна' || 'пошта');

// Стало (правильно):
pm.expect(error.message).to.include('електронну адресу');
```

### 🎯 **Детальні інструкції по налагодженню:**

#### **Крок 1: Перевірка налаштувань середовища**

**Для локального тестування:**

```
baseUrl = http://localhost:3000
```

**Для WSL (Windows + Postman):**

```bash
# Знайдіть IP адресу WSL:
hostname -I | awk '{print $1}'
# Використовуйте результат (наприклад, 172.28.183.155):
baseUrl = http://172.28.183.155:3000
```

#### **Крок 2: Переконайтеся, що сервер запущений**

```bash
npm start
# Або
node src/app.js
```

#### **Крок 3: Оптимальний порядок запуску тестів**

**Опція А: Послідовний запуск (рекомендовано)**

1. Запустіть запити по одному у правильному порядку:
   - 📋 Отримати всі контакти
   - 📄 Отримати контакт за ID
   - ➕ Створити новий контакт
   - 🔄 Оновити контакт
   - 🗑️ Видалити контакт

**Опція Б: Collection Runner**

1. Клікніть на колекцію → Run
2. Виберіть всі запити
3. Встановіть затримку 500ms між запитами
4. Запустіть

### 🎯 **Очікувані результати:**

#### ✅ **Що повинно працювати:**

- **GET /api/contacts** → 200 OK, масив контактів
- **GET /api/contacts/ID** → 200 OK, один контакт
- **POST /api/contacts** → 201 Created, новий контакт
- **PUT /api/contacts/ID** → 200 OK, оновлений контакт
- **DELETE /api/contacts/ID** → 200 OK, видалений контакт
- **Валідація помилок** → 400 Bad Request з описом

#### ❌ **Типові проблеми та рішення:**

**1. Сервер недоступний**

```
Error: getaddrinfo ENOTFOUND localhost
```

**Рішення:** Перевірте, чи запущений сервер та правильність `baseUrl`

**2. CORS помилки**

```text
Access to fetch blocked by CORS policy
```

**Рішення:** Переконайтеся, що CORS middleware налаштований у Express

**3. WSL мережеві проблеми**

```
Error: connect ETIMEDOUT
```

**Рішення:** Використовуйте IP адресу WSL замість localhost

**4. 404 Not Found для операцій з ID:**

- Перевірте, чи встановлена змінна `testContactId`
- Використовуйте реальний ID з бази даних: `AeHIrLTr6JkxGE6SN-0Rw`

**5. Connection refused:**

- Переконайтеся, що сервер запущений (`npm start`)
- Перевірте правильність базової URL

**6. Валідація не працює:**

- Оновлена колекція тепер перевіряє точні українські повідомлення про помилки

### 🔍 **Діагностика через командний рядок**

#### Перевірка через curl

```bash
# Тест 1: Всі контакти
curl http://localhost:3000/api/contacts

# Тест 2: Контакт за ID
curl http://localhost:3000/api/contacts/AeHIrLTr6JkxGE6SN-0Rw

# Тест 3: Створення з валідацією
curl -X POST http://localhost:3000/api/contacts \
  -H "Content-Type: application/json" \
  -d '{"name":"Test","email":"invalid","phone":"+380501234567"}'
```

#### Логи сервера

Під час тестування перевіряйте логи сервера:

```bash
# У терміналі WSL/Linux
npm run dev

# Очікувані логи:
# GET /api/contacts 200 1321 - 10.504 ms
# POST /api/contacts 201 122 - 11.415 ms
```

## 📚 Додаткові ресурси

💡 **Підказка**: Якщо якісь тести все ще падають, використовуйте Console в Postman для діагностики змінних та відповідей сервера.

- [Postman Documentation](https://learning.postman.com/)
- [WSL Networking](https://docs.microsoft.com/en-us/windows/wsl/networking)
- [E.164 Phone Number Format](https://en.wikipedia.org/wiki/E.164)
- [Express.js CORS](https://expressjs.com/en/resources/middleware/cors.html)

---

**🎓 Створено для курсу GoIT "Fullstack. Back End Development: Node.js"**
**📅 Тема 4: REST API**
